import type { FC } from 'react'
import React, { memo } from 'react'

const DeMasterRef: FC = () => {
  return (
    <div>
      <h1>DeMasterRef</h1>
    </div>
  )
}

export default memo(DeMasterRef)
